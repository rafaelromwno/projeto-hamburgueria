<nav class="navbar navbar-expand-sm navbar-dark bg-dark border-bottom box-shadow mb-3">
    <div class="container">

        <!-- Logo e Marca -->
        <a class="navbar-brand d-flex align-items-center" asp-area="" asp-controller="Home" asp-action="Index">
            <img src="~/images/logo_lanche2-sfd.png" alt="Logo LanchesMac" class="me-2" />
            LanchesMac
        </a>

        <!-- Botão para Mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar"
                aria-controls="mainNavbar" aria-expanded="false" aria-label="Alternar Navegação">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Menu Principal -->
        <div class="collapse navbar-collapse" id="mainNavbar">
            <ul class="navbar-nav ms-auto mb-2 mb-sm-0">
                <!-- Componentes Dinâmicos -->
                <li class="nav-item">
                    @await Component.InvokeAsync("CategoriaMenu")
                </li>

                <!-- Campo de Busca -->
                <li class="nav-item">
                    <form class="d-flex" asp-controller="Lanche" asp-action="Search" method="get">
                        <div class="input-group">
                            <input type="text" class="form-control" placeholder="Procurar produto" name="searchString"
                                   value="@ViewData["searchString"]" aria-label="Procurar produto">
                        </div>
                    </form>
                </li>

                <!-- Links de Navegação -->
                <li class="nav-item">
                    <a class="nav-link text-light" asp-area="" asp-controller="Contato" asp-action="Index">Contato</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-light" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                </li>

                <!-- Link Administrativo (Somente para Admins) -->
                @if (User.IsInRole("Admin"))
                {
                    <li class="nav-item">
                        <a class="nav-link text-light" asp-area="Admin" asp-controller="Admin" asp-action="Index">Admin</a>
                    </li>
                }

                <!-- Login e Carrinho -->
                <li class="nav-item">
                    @await Html.PartialAsync("_LoginPartial")
                </li>
                <li class="nav-item">
                    @await Component.InvokeAsync("CarrinhoCompraResumo")
                </li>
            </ul>
        </div>
    </div>
</nav>
